Generate a single horizontal game UI button asset.

Use the reference image as the exact style target:
- Same flat comic style.
- Same pill button shape.
- Same proportions.
- Same thick blue outline style.
- Same thick white inner border.
- Same simple glossy highlight band.
- Same flat clean edges.
- Same Chinese text style, stroke thickness, and spacing.

Canvas:
- 4:3 PNG canvas.
- Use a pure flat chroma green background outside the button: RGB #00FF00.
- Put exactly one wide horizontal pill button in the center.
- The button should occupy about 88% of canvas width and 30% of canvas height.
- NO cast shadow, NO drop shadow, NO fuzzy transparent shadow, NO glow outside the button.

Button color:
- Must pair well with the reference blue "下一关" button.
- Use a bright warm color, more cheerful than the previous dull orange: sunny orange / golden yellow-orange main fill.
- Keep the same blue outer outline from the reference so it visually belongs with the blue next-level button.
- Keep the thick white inner ring exactly like the reference.

Text:
- Replace the reference text with EXACTLY: 回到首页
- The four Chinese characters must be continuous with NO spaces: 回到首页
- Do not write 回到主页.
- White text fill with the same blue outline style as the reference text.
- Text size, weight, rounded font style, and spacing should match the reference "下一关" button as closely as possible.

Strict constraints:
- Generate ONLY the Chinese string: 回到首页
- NO English, NO numbers, NO watermark, NO logo, NO extra labels.
- Do not add a house icon.
- Single finished button asset only.
