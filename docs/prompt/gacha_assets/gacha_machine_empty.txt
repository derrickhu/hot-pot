Single gachapon (capsule toy) machine asset, transparent background, 9:16 canvas,
the EMPTY-DOME version (no capsule balls inside the dome at all).
Reference image is the full UI mockup — keep EXACTLY the same machine shape, colours,
outline thickness, decorations:

- Top: a small golden round logo with a star inside (the same as the mockup).
- Below it: yellow + red circus-tent style pointed cap on a red rim.
- Middle: a LARGE round glass dome — light cyan-blue tint (#b7f1ff), thick white outline,
  big diagonal white highlight on the upper-left, faint shadow arc on the lower-right.
  The DOME INTERIOR IS COMPLETELY EMPTY — no balls, no capsules, no objects, no sparkles
  inside the dome glass. Just the empty bluish glass.
- Lower body: chunky red rounded base (#ff5b6f), with the EXACTLY two-character Chinese
  label 扭蛋 baked into the body in huahua / 叮咚 POP yellow-fill / red outline / brown
  outermost outline cartoon font (same as the mockup). Two small golden side knobs.
- Egg slot: bottom yellow half-window with a SMALL semicircular opening below it.
  The opening must be EMPTY — NO golden egg currently sitting inside. Just the empty
  honey-yellow plastic shell.
- Right side: a golden crank handle ("J" shape, red ball at the end), tilted toward
  ~1 o'clock position, a small clockwise rotation arrow next to it.
- A few small decorative stars and burst lines on the lower-right beside the handle.
- Soft elliptical shadow under the whole machine.

STRICT constraints:

- TRANSPARENT background.
- The dome MUST be VISUALLY EMPTY (you can still see the dome volume / glass),
  but no balls/capsules/objects/sparkle dots inside it.
- The egg slot MUST be VISUALLY EMPTY (no golden egg).
- Generate ONLY the Chinese characters 扭蛋 (exactly two characters) on the machine body.
  No other text. No English. No numbers. No QR code. No watermark.
- NO speech bubbles, NO question marks, NO surrounding fruits / sparkle decoration outside
  the immediate machine area (handle starbursts are fine).
- Single coherent machine sprite, ready to be composited on top of the bg layer.
